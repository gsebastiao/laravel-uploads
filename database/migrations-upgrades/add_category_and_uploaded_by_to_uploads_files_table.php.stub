<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * Migration de upgrade — apenas para quem já tinha a v1.0.0 instalada.
 *
 * Instalações novas, a partir da v1.1.0, não precisam disto: a migration
 * original do pacote já cria estas colunas desde o início. Publica-se
 * separadamente (php artisan vendor:publish --tag=uploads-upgrade-1.1.0)
 * precisamente para não ser aplicada a instalações novas por engano — e,
 * mesmo assim, as guardas Schema::hasColumn() abaixo tornam-na segura de
 * correr mesmo que isso aconteça.
 */
return new class extends Migration
{
    /**
     * Executa a migration.
     */
    public function up(): void
    {
        Schema::table('uploads_files', function (Blueprint $table): void {
            if (! Schema::hasColumn('uploads_files', 'category')) {
                $table->string('category')->nullable()->after('reference_id');
                $table->index('category');
            }

            if (! Schema::hasColumn('uploads_files', 'uploaded_by')) {
                $table->unsignedBigInteger('uploaded_by')->nullable()->after('category');
                $table->index('uploaded_by');
            }
        });
    }

    /**
     * Reverte a migration.
     */
    public function down(): void
    {
        Schema::table('uploads_files', function (Blueprint $table): void {
            if (Schema::hasColumn('uploads_files', 'category')) {
                $table->dropIndex(['category']);
                $table->dropColumn('category');
            }

            if (Schema::hasColumn('uploads_files', 'uploaded_by')) {
                $table->dropIndex(['uploaded_by']);
                $table->dropColumn('uploaded_by');
            }
        });
    }
};
